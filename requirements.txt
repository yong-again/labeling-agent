# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0
requests>=2.31.0
python-dotenv>=1.0.0

# Grounding DINO
# Note: groundingdino-py는 별도 설치 필요
# pip install groundingdino-py
# 또는 소스에서 설치:
# git clone https://github.com/IDEA-Research/GroundingDINO.git
# cd GroundingDINO
# pip install -e .

# SAM (Segment Anything Model)
# Note: segment-anything는 별도 설치 필요
# pip install git+https://github.com/facebookresearch/segment-anything.git

